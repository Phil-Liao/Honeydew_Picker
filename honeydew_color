import cv2
frame = cv2.imread("Assets/Honeydew_1.jpeg", -1)
#print(frame[332:, 115:882])
#print(len(frame))
color_lst = []
#print(frame[0])
for i in frame:
    for j in i:
        #print(j)
        if tuple(j) not in color_lst:
            print(tuple(j))
            color_lst.append(tuple(j))
            
print(color_lst)
                        